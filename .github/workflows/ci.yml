name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - master
      - scheduling
  pull_request:
    branches:
      - main
      - master
      - scheduling

jobs:
  lint-and-test:
    name: Lint & Unit Test (${{ matrix.service }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - sms-identity
          - sms-management
          - sms-monitoring
          - sms-reporting

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Patch Go version for CI
        run: |
          sed -i 's/go 1.26.2/go 1.24/g' ${{ matrix.service }}/go.mod

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Go Mod Tidy
        working-directory: ${{ matrix.service }}
        run: |
          go mod tidy
          sed -i -E 's/go 1\.[0-9]+(\.[0-9]+)?/go 1.24/g' go.mod

      - name: Get Go Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ matrix.service }}-${{ hashFiles(format('{0}/go.sum', matrix.service)) }}
          restore-keys: |
            ${{ runner.os }}-go-${{ matrix.service }}-

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: ${{ matrix.service }}
          args: --timeout=10m

      - name: Run Unit Tests with Coverage
        working-directory: ${{ matrix.service }}
        run: |
          go test -coverprofile=c.out ./...
          echo "mode: set" > core.out
          grep -E "internal/service|internal/repository/impl|internal/handler/grpc|internal/worker|internal/consumer" c.out | grep -v -E "mock|middlewares" >> core.out || true
          echo "=== Core Business Logic Coverage ==="
          go tool cover -func=core.out

  integration-test:
    name: Integration Test (Bruno)
    needs: lint-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Patch Go version for CI
        run: |
          find . -name "go.mod" -exec sed -i 's/go 1.26.2/go 1.24/g' {} +
          find . -name "Dockerfile" -exec sed -i 's/golang:1.26.2-alpine/golang:1.24-alpine/g' {} +

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run Integration Tests
        shell: pwsh
        env:
          COMPOSE_PROJECT_NAME: sms
        run: |
          cd bruno
          ./run_tests.ps1 -Target ci
