meta {
  name: View Servers - Filter by Name
  type: http
  seq: 23
}

get {
  auth: bearer
  url: {{baseURL}}/api/v1/servers?page=1&limit=10&filterName=bruno
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
