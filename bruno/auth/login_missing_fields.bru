meta {
  name: Login - Missing Password (400)
  type: http
  seq: 7
}

post {
  url: {{baseURL}}/api/v1/auth/login
  body: json
}

body:json {
  {
    "identifier": "{{adminEmail}}"
  }
}

assert {
  res.status: gte 400
}
